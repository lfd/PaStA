#!/bin/bash

function die {
  echo "$@" 1>&2;
  exit -1;
}

echo "This may take a while. Take a cup of coffe. Really."

git submodule init || die "submodule init failed"
git submodule update || die "submodule update failed"

cd PaStA-resources
./prepare_projects
